% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcano.R
\name{togo_plot_volcano}
\alias{togo_plot_volcano}
\title{Volcano plot of differential-expression results}
\usage{
togo_plot_volcano(
  data,
  fc_col,
  p_col,
  label_col = "Gene",
  title = NULL,
  x_lab = fc_col,
  y_lab = "-log10(p)",
  p_thresh = 0.05,
  positive_text = "Up",
  negative_text = "Down",
  n_label = 20,
  genes_to_label = NULL,
  fc_limit = 10,
  pos_color = "#f28482",
  neg_color = "#457b9d",
  ns_color = "#ced4da",
  text_size = 15,
  point_size = 3,
  label_size = 3
)
}
\arguments{
\item{data}{Data frame of results.}

\item{fc_col}{Column with effect size / log fold-change.}

\item{p_col}{Column with the p-value (or adjusted p-value) to plot on -log10 y.}

\item{label_col}{Column with point labels (e.g. gene symbols). Default \code{"Gene"}.}

\item{title}{Plot title.}

\item{x_lab, y_lab}{Axis labels.}

\item{p_thresh}{Significance threshold (horizontal dashed line). Default \code{0.05}.}

\item{positive_text, negative_text}{Arrow annotations for the up/down directions.}

\item{n_label}{Max genes to label per direction. Default \code{20}.}

\item{genes_to_label}{Optional explicit vector of labels to restrict labeling to.}

\item{fc_limit}{Drop points with \code{abs(fc) >= fc_limit} before plotting. Default \code{10}.}

\item{pos_color, neg_color, ns_color}{Point colors for up / down / non-significant.}

\item{text_size, point_size, label_size}{Sizing controls.}
}
\value{
A ggplot object.
}
\description{
A general volcano plot: colors points by significance and direction, labels
the top genes in each direction, and draws direction arrows along the x-axis.
Generalized from the lab's ATTEMPT volcano (no treatment-specific defaults).
}
\examples{
\dontrun{
togo_plot_volcano(res, fc_col = "logFC", p_col = "fdr",
                  x_lab = "log2 FC", y_lab = "-log10(FDR)")
}
}
