% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrna.R
\name{togo_run_nebula_parallel}
\alias{togo_run_nebula_parallel}
\title{NEBULA differential expression in parallel, per gene}
\usage{
togo_run_nebula_parallel(
  object,
  formula = ~group,
  id_col = "record_id",
  offset_col = "pooled_offset",
  layer = "counts",
  n_cores = 4,
  group = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object.}

\item{formula}{Model formula (default \code{~ group}).}

\item{id_col}{Subject/grouping id column. Default \code{"record_id"}.}

\item{offset_col}{Optional per-cell offset column. Default \code{"pooled_offset"}.}

\item{layer}{Counts layer. Default \code{"counts"}.}

\item{n_cores}{Number of parallel workers.}

\item{group}{Whether to call \code{\link[nebula:group_cell]{nebula::group_cell()}} (set \code{FALSE} if already grouped).}

\item{verbose}{Print per-gene warnings/errors and timing.}
}
\value{
A list: \code{results} (named list of per-gene nebula fits),
\code{n_genes_tested}, \code{n_genes_converged}, \code{nonconverged_percent},
\code{runtime_minutes}.
}
\description{
Fits NEBULA per gene in parallel (one gene per task), returning a named list
of per-gene results plus run metadata. Optionally uploads the results to S3
via an aws.s3-style client. Design is fully formula-driven and generalizes
across datasets.
}
