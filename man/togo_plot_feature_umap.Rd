% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umap.R
\name{togo_plot_feature_umap}
\alias{togo_plot_feature_umap}
\title{Feature UMAP plots highlighting expressing cell types}
\usage{
togo_plot_feature_umap(
  genes,
  metadata,
  centers,
  celltype_colors,
  celltype_col = "KPMP_celltype",
  pct_threshold = 10,
  save_fun = NULL,
  plot_height = 10,
  plot_width = 10
)
}
\arguments{
\item{genes}{Character vector of genes to plot.}

\item{metadata}{Data frame from \code{\link[=togo_prepare_umap_metadata]{togo_prepare_umap_metadata()}} (\verb{$metadata}).}

\item{centers}{Label-center data frame (\verb{$centers}) with \code{x}, \code{y}, and the
cell-type column.}

\item{celltype_colors}{Named vector of cell-type colors (\verb{$celltype_colors}).}

\item{celltype_col}{Cell-type column name (must match \code{centers}). Default \code{"KPMP_celltype"}.}

\item{pct_threshold}{Minimum percent expressed to highlight/label a cell type.}

\item{save_fun}{Optional function \verb{(plot, gene) -> NULL} to save each plot
(e.g. wrap \code{\link[=togo_s3_save_plot]{togo_s3_save_plot()}} or \code{ggplot2::ggsave}). \code{NULL} to skip saving.}

\item{plot_height, plot_width}{Passed to \code{save_fun} via attributes (height/width).}
}
\value{
Invisibly, a named list of ggplot objects (one per gene).
}
\description{
For each gene, draws a UMAP where cells expressing the gene are colored by
cell type (others greyed out), labels cell types passing an expression
threshold, and adds a markdown caption of percent-expressed per cell type.
}
