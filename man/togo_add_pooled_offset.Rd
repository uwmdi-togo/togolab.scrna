% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/offset.R
\name{togo_add_pooled_offset}
\alias{togo_add_pooled_offset}
\title{Add a pooled-library size-factor offset to a Seurat object}
\usage{
togo_add_pooled_offset(
  so,
  layer = "counts",
  offset_col = "pooled_offset",
  stochastic = FALSE,
  seed = 1,
  workers = 1,
  clusters = NULL,
  BPPARAM = NULL
)
}
\arguments{
\item{so}{A Seurat object.}

\item{layer}{Assay layer holding counts (Seurat v5). Default \code{"counts"}.}

\item{offset_col}{Metadata column to store the size factors in. Default
\code{"pooled_offset"}.}

\item{stochastic}{If \code{TRUE}, use stochastic rounding (see Details). Default \code{FALSE}.}

\item{seed}{Seed used when \code{stochastic = TRUE}, for reproducibility. Default \code{1}.}

\item{workers}{Number of parallel workers for \code{\link[scran:computeSumFactors]{scran::computeSumFactors()}}.
\code{1} (default) runs serially; \verb{>1} uses \code{\link[BiocParallel:MulticoreParam-class]{BiocParallel::MulticoreParam()}}.}

\item{clusters}{Optional cluster assignment passed to
\code{\link[scran:computeSumFactors]{scran::computeSumFactors()}} (e.g. from \code{scran::quickCluster()}); \code{NULL}
(default) pools across all cells.}

\item{BPPARAM}{Optional \code{BiocParallelParam} to override \code{workers} entirely.}
}
\value{
The Seurat object with the size-factor offset added to \code{meta.data}.
}
\description{
Computes scran pooled size factors (\code{scran::computeSumFactors()}) from the
counts matrix and stores them as a metadata column (default \code{pooled_offset}),
for use as the offset in NEBULA / negative-binomial models
(see \code{\link[=togo_run_nebula]{togo_run_nebula()}}).
}
\details{
Counts are rounded to integers first. With \code{stochastic = FALSE} (default)
this is ordinary rounding; with \code{stochastic = TRUE} it uses stochastic
rounding, \code{floor(x) + rbinom(n, 1, x - floor(x))}, seeded for reproducibility
— useful when counts are non-integer (e.g. ambient-corrected) and you want to
preserve expected totals.
}
\examples{
\dontrun{
so <- togo_add_pooled_offset(so)                       # standard rounding
so <- togo_add_pooled_offset(so, stochastic = TRUE, seed = 42)
so <- togo_add_pooled_offset(so, workers = 63)         # parallel
fit <- togo_run_nebula(~ group, so, offset_col = "pooled_offset")
}
}
