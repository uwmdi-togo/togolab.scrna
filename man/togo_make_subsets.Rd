% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrna.R
\name{togo_make_subsets}
\alias{togo_make_subsets}
\title{Subset a Seurat object by cell-type groups}
\usage{
togo_make_subsets(
  object,
  groups,
  celltype_col = "celltype",
  assign_global = FALSE,
  prefix = "so_"
)
}
\arguments{
\item{object}{A Seurat object.}

\item{groups}{Named list mapping a group name to a vector of cell-type labels.}

\item{celltype_col}{Column in \code{object@meta.data} holding cell-type labels.}

\item{assign_global}{If \code{TRUE}, also assign each subset into the global
environment as \verb{<prefix><group>} (legacy behavior). Default \code{FALSE}.}

\item{prefix}{Object-name prefix used when \code{assign_global = TRUE}.}
}
\value{
A list with \code{subsets} (named list of Seurat objects) and \code{summary}
(data frame of group, n_cells).
}
\description{
Splits a Seurat object into a named list of subsets, one per group of cell
types. Generalizes across datasets via the \code{celltype_col} argument.
}
