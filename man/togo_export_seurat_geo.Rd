% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo_export.R
\name{togo_export_seurat_geo}
\alias{togo_export_seurat_geo}
\title{Export a Seurat object to per-participant processed files on S3 (GEO format)}
\usage{
togo_export_seurat_geo(
  seurat_object,
  project_path,
  bucket = "togo.projects",
  region = "",
  id_col = "orig.ident",
  layer = "counts",
  meta_cols = NULL,
  suffix = "_processed",
  write_manifest = TRUE
)
}
\arguments{
\item{seurat_object}{A Seurat object.}

\item{project_path}{S3 key prefix (folder) to write under, e.g.
\code{"P001/clean_data/GEO_ProcessedData"}.}

\item{bucket}{S3 bucket. Default \code{"togo.projects"}.}

\item{region}{S3 region. Default \code{""} (Kopah).}

\item{id_col}{Metadata column identifying participants. Default \code{"orig.ident"}.}

\item{layer}{Assay layer holding counts (Seurat v5). Default \code{"counts"}.}

\item{meta_cols}{Optional character vector of \code{meta.data} columns to attach
to a second, metadata-joined manifest (e.g. \code{c("visit", "treatment")}).
\code{NULL} (default) skips the metadata manifest.}

\item{suffix}{Filename suffix for the processed files. Default \code{"_processed"}.}

\item{write_manifest}{If \code{TRUE} (default), upload the file-info CSV(s).}
}
\value{
Invisibly, the file-info \code{data.frame} (participant IDs, file names,
MD5 checksums).
}
\description{
For each participant in a Seurat object, writes the counts matrix
(Matrix Market \code{.mtx}), cell barcodes (\code{.tsv}), and gene features (\code{.tsv})
to the lab S3 store, computes MD5 checksums, and uploads a CSV manifest of
files + checksums. Everything is read from / written to S3 — no local
directories are created (temp files are used and cleaned up).
}
\details{
Generalized from the lab's ATTEMPT GEO-export script: works on any Seurat
object, with a configurable participant-ID column and optional metadata
columns to attach to the manifest. All uploads route through
\code{togolab::s3write_using_region()} so \code{region} is always passed (Kopah
requires \code{region = ""}).

Assumes S3 credentials are configured (run \code{togolab::togo_paths()} first).
}
\examples{
\dontrun{
library(togolab); togo_paths()
so <- togolab::togo_s3_read_rds("P001/clean_data/example.rds",
                                bucket = "togo.projects")
togo_export_seurat_geo(so,
                       project_path = "P001/clean_data/GEO_ProcessedData",
                       meta_cols = c("visit", "treatment"))
}
}
